<nav class="navbar navbar-expand-lg navbar-main py-3">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <img src="{{ asset('images/logo.svg') }}" alt="Carrosserie Lino" height="50">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMain" aria-controls="navMain" aria-expanded="false" aria-label="Menu">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navMain">
            <ul class="navbar-nav ms-auto align-items-center">
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') == 'app_home' ? 'active' : '' }}" href="{{ path('app_home') }}">
                        {{ 'nav.home'|trans }}
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') == 'app_qui_sommes_nous' ? 'active' : '' }}" href="{{ path('app_qui_sommes_nous') }}">
                        {{ 'nav.about'|trans }}
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}#services">
                        {{ 'nav.services'|trans }}
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') == 'app_gallery' ? 'active' : '' }}" href="{{ path('app_gallery') }}">
                        {{ 'nav.gallery'|trans }}
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}#avis">
                        {{ 'nav.reviews'|trans }}
                    </a>
                </li>
                <li class="nav-item ms-3">
                    {% set currentLocale = app.request.locale starts with 'de' ? 'de' : 'fr' %}
                    <div class="btn-group btn-group-sm" role="group" aria-label="{{ 'nav.lang_selector_label'|trans }}">
                        <a href="{{ path('app_switch_locale', { locale: 'fr' }) }}"
                           class="btn {{ currentLocale == 'fr' ? 'btn-primary' : 'btn-outline-primary' }}">
                            FR
                        </a>
                        <a href="{{ path('app_switch_locale', { locale: 'de' }) }}"
                           class="btn {{ currentLocale == 'de' ? 'btn-primary' : 'btn-outline-primary' }}">
                            DE
                        </a>
                    </div>
                </li>
                <li class="nav-item ms-2">
                    <a class="btn btn-devis rounded-pill" href="{{ path('app_devis') }}">
                        {{ 'nav.quote'|trans }}
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>
<div class="contact-bar py-2">
    <div class="container">
        <div class="row align-items-center justify-content-between">
            <div class="col-auto d-flex flex-wrap gap-4">
                <span>📍 {{ 'home.contact.location'|trans }}</span>
                <a href="tel:0389572828">📞 03 89 57 28 28</a>
                <span>
                    {% if horaires_compact is defined and horaires_compact is not empty %}
                        🕐
                        <a href="{{ path('app_horaires_public') }}" class="ms-1 text-decoration-none text-reset">
                            {% include 'components/_opening_hours_compact.html.twig' %}
                        </a>
                    {% endif %}
                </span>
            </div>
        </div>
    </div>
</div>

